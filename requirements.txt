#this is package.json on node

Django>=4.0,<5.0 #specify which django version, in this case at least 4.0, but not as new as 5.0
psycopg2>=2.8 #driver
django-extensions>=3.0,<4.0 #extensions 
django-environ==0.9.0